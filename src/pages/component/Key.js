const Key = ({ children }) => {
  return (
    <button className="text-lg font-bold uppercase bg-gray-200 min-w-[45px] h-[55px] rounded px-3">
      {children}
    </button>
  );
};

export default Key;
